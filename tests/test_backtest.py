"""
Unit tests for src/backtest.py.

To be filled in once run_backtest and compute_metrics are implemented. Example
cases to cover:
    - a flat (all-zero) signal produces zero P&L
    - transaction costs are deducted exactly on position-change days
    - compute_metrics matches hand-calculated Sharpe/drawdown on a toy series
"""
