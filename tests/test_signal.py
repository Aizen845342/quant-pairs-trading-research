"""
Unit tests for src/signal.py.

To be filled in once compute_hedge_ratio, compute_spread, compute_rolling_zscore,
and generate_signal are implemented. Example cases to cover:
    - compute_hedge_ratio recovers a known beta from synthetic data
    - compute_rolling_zscore returns NaN before the window is full
    - generate_signal enters at +/-2 and exits at 0, exactly at threshold crossings
"""
